/**
 * Shared "underline" look for composer fields (title, summary, slug,
 * category, commit message, …) — no box, just a bottom border that lights
 * up on focus. Compose with per-field size/weight utilities.
 */
export const underlineFieldClass =
  "bg-transparent border-0 border-b border-rim outline-none focus:border-accent transition-colors";
