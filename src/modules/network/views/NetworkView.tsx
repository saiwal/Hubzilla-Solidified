// The page title and post stream are registered as widgets in ../index.ts
// (header / contentTop slots) and rendered by Layout.tsx's <Slot .../>
// around this view — see hq/views/HqView.tsx for the same pattern.
export default function NetworkView() {
  return null;
}
