// The profile card + "back to channel" link are registered as a widget in
// ../index.ts (contentTop slot) and rendered by Layout.tsx's <Slot .../>
// around this view — see hq/views/HqView.tsx for the same pattern.
export default function ProfilePageView() {
  return null;
}
