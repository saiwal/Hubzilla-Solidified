// The channel-details card and post feed are registered as widgets in
// ../index.ts (contentTop / contentBottom slots) and rendered by
// Layout.tsx's <Slot name="contentTop"/"contentBottom" .../> around this
// view — see hq/views/HqView.tsx for the same pattern (gridTop there).
export default function ChannelView() {
  return null;
}
